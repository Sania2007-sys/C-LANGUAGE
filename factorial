#include <stdio.h>

unsigned long long factorial(int n){
    if(n<0){
        return 0;
    }
    if(n==0||n==1){
        return 1;
    }
    return n *factorial(n-1);
}
int main(){
    int num=5;
    printf("factorial of %d is :%11u\n",num,factorial(num));
    return 0;
}
