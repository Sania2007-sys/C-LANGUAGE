#include <stdio.h>

int main()

{
    int a,b,big;
    printf("Enter two numbers:");
    scanf("%d%d",&a,&b);
    big=((a>b)?a:b);
    while(big%a!=0||big%b!=0)
    {
        big++;
    }
    printf("LCM is %d",big);
    
    return 0;
}
